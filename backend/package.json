{
  "name": "expense-tracker",
  "version": "4.19.0",
  "description": "Full-stack Expense Tracker Application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd client && npm install && npm run build",
    "db:check": "node scripts/checkDatabaseSchema.js",
    "db:migrate": "node scripts/addFixedExpensesTable.js",
    "test": "cross-env NODE_ENV=test jest --runInBand",
    "test:fast": "cross-env NODE_ENV=test FAST_CHECK_NUM_RUNS=10 jest --runInBand",
    "test:unit": "cross-env NODE_ENV=test jest --runInBand --testPathIgnorePatterns=pbt",
    "test:pbt": "cross-env NODE_ENV=test jest --runInBand --testNamePattern=pbt",
    "test:ci": "cross-env NODE_ENV=test CI=true FAST_CHECK_NUM_RUNS=25 jest --runInBand --bail"
  },
  "jest": {
    "testTimeout": 30000,
    "globalSetup": "<rootDir>/jest.globalSetup.js",
    "setupFilesAfterEnv": [
      "<rootDir>/jest.setup.js"
    ],
    "testEnvironment": "node",
    "verbose": false,
    "forceExit": true
  },
  "dependencies": {
    "body-parser": "^2.2.2",
    "cors": "^2.8.6",
    "csv-parser": "^3.2.0",
    "express": "^5.2.1",
    "express-rate-limit": "^8.2.1",
    "helmet": "^8.1.0",
    "multer": "^2.0.2",
    "sqlite3": "^5.1.6",
    "tar": "^7.5.6"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "baseline-browser-mapping": "^2.9.18",
    "cross-env": "^10.1.0",
    "fast-check": "^4.5.3",
    "jest": "^30.2.0",
    "nodemon": "^3.0.1",
    "supertest": "^7.2.2"
  }
}
