{
  "overridePattern": "\\[skip-budget\\]",
  "budgets": {
    "backend-unit-tests": {
      "maxSeconds": 90,
      "description": "Backend unit tests (backup suites run in backend-pbt shard 1)"
    },
    "backend-pbt-shard": {
      "maxSeconds": 90,
      "description": "Backend PBT shard (FAST_CHECK_NUM_RUNS=15)"
    },
    "frontend-tests": {
      "maxSeconds": 360,
      "description": "Frontend Vitest suite (parallel forks)"
    }
  }
}
